name: VPS Setup

on:
  workflow_dispatch:

jobs:
  setup-vps:
    runs-on: windows-latest
    timeout-minutes: 20

    steps:
      - name: ⚙️ Set up job
        run: Write-Host "Inicializando ambiente Windows..."

      # ---------------------------
      # 1️⃣ Baixar repositório
      # ---------------------------
      - name: 📦 Baixar repositório
        uses: actions/checkout@v4

      # ---------------------------
      # 2️⃣ Restaurar backup (opcional)
      # ---------------------------
      - name: 💾 Restaurar backup anterior (Área de Trabalho)
        shell: pwsh
        run: |
          $backup = "$env:USERPROFILE\Desktop_Backup"
          $desktop = "$env:USERPROFILE\Desktop"
          if (Test-Path $backup) {
            Write-Host "🔄 Restaurando backup anterior..."
            Copy-Item -Path "$backup\*" -Destination $desktop -Recurse -Force
          } else {
            Write-Host "ℹ️ Nenhum backup anterior encontrado."
          }

      # ---------------------------
      # 3️⃣ Conectar ao Tailscale
      # ---------------------------
      - name: 🔗 Conectar ao Tailscale
        shell: pwsh
        run: |
          Write-Host "🚀 Instalando Tailscale via winget..."
          winget install --id=Tailscale.Tailscale --accept-package-agreements --accept-source-agreements -h | Out-Null

          $tailscalePath = "C:\Program Files\Tailscale\tailscale.exe"
          $authkey = '${{ secrets.TAILSCALE_KEY }}'

          if (-not (Test-Path $tailscalePath)) {
            throw "❌ Tailscale não foi instalado corretamente!"
          }

          Write-Host "🔗 Conectando ao Tailscale..."
          & "$tailscalePath" up --authkey "$authkey" --hostname "vps-auto" --accept-routes --ssh
          if ($LASTEXITCODE -ne 0) {
            throw "❌ Falha ao conectar ao Tailscale"
          }

          Write-Host "✅ Conectado ao Tailscale com sucesso!"
          & "$tailscalePath" ip -4

      # ---------------------------
      # 4️⃣ Definir senha fixa (para RDP)
      # ---------------------------
      - name: 🔐 Definir senha fixa
        shell: pwsh
        run: |
          $password = ConvertTo-SecureString '${{ secrets.RDP_PASSWORD }}' -AsPlainText -Force
          $username = "runneradmin"
          Set-LocalUser -Name $username -Password $password
          Write-Host "✅ Senha fixa definida para $username"

      # ---------------------------
      # 5️⃣ Mostrar credenciais RDP
      # ---------------------------
      - name: 🪪 Mostrar credenciais RDP
        shell: pwsh
        run: |
          $ip = (Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.InterfaceAlias -eq "Ethernet" }).IPAddress
          Write-Host "🖥️ Use as credenciais abaixo para conectar via RDP:"
          Write-Host "Usuário: runneradmin"
          Write-Host "Senha: ${{ secrets.RDP_PASSWORD }}"
          Write-Host "IP Local: $ip"
          Write-Host "🔗 IP Tailscale (recomendado):"
          & "C:\Program Files\Tailscale\tailscale.exe" ip -4

      # ---------------------------
      # 6️⃣ Manter VPS ativa
      # ---------------------------
      - name: ♻️ Mantendo VPS ativa
        shell: pwsh
        run: |
          Write-Host "⏳ Mantendo VPS ativa por tempo indeterminado..."
          while ($true) {
            Start-Sleep -Seconds 60
          }

      # ---------------------------
      # 7️⃣ Fazer backup final da Área de Trabalho
      # ---------------------------
      - name: 💾 Fazer backup da Área de Trabalho
        if: always()
        shell: pwsh
        run: |
          $backup = "$env:USERPROFILE\Desktop_Backup"
          $desktop = "$env:USERPROFILE\Desktop"
          New-Item -ItemType Directory -Force -Path $backup | Out-Null
          Copy-Item -Path "$desktop\*" -Destination $backup -Recurse -Force
          Write-Host "✅ Backup salvo em $backup"
