name: RDP + Tailscale AutoConnect

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      # ---------------------------
      # 1️⃣ Configurações principais do RDP
      # ---------------------------
      - name: 🖥️ Configurar RDP
        shell: pwsh
        run: |
          # Ativar Remote Desktop
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                           -Name "fDenyTSConnections" -Value 0 -Force

          # Desativar autenticação de nível de rede
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                           -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                           -Name "SecurityLayer" -Value 0 -Force

          # Liberar firewall para conexões RDP
          netsh advfirewall firewall delete rule name="RDP-Tailscale" | Out-Null
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389

          # Reiniciar serviço RDP
          Restart-Service -Name TermService -Force
          Write-Host "✅ RDP habilitado e firewall configurado"

      # ---------------------------
      # 2️⃣ Criar usuário de acesso RDP
      # ---------------------------
      - name: 👤 Criar usuário RDP
        shell: pwsh
        run: |
          $user = "RDP"
          $password = '${{ secrets.RDP_PASSWORD }}'

          if (-not $password) {
            Write-Error "❌ ERRO: Nenhuma senha definida em RDP_PASSWORD (secrets)"
            exit 1
          }

          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          if (Get-LocalUser -Name $user -ErrorAction SilentlyContinue) {
            Write-Host "ℹ️ Usuário RDP já existe, redefinindo senha..."
            Set-LocalUser -Name $user -Password $securePass
          } else {
            New-LocalUser -Name $user -Password $securePass -AccountNeverExpires
            Add-LocalGroupMember -Group "Administrators" -Member $user
            Add-LocalGroupMember -Group "Remote Desktop Users" -Member $user
          }

          Write-Host "✅ Usuário '$user' criado e habilitado para RDP"
          echo "RDP_CREDS=User: $user | Password: $password" >> $env:GITHUB_ENV

      # ---------------------------
      # 3️⃣ Instalar e conectar Tailscale automaticamente (sem login manual)
      # ---------------------------
      - name: 🌐 Conectar ao Tailscale
        shell: pwsh
        run: |
          Write-Host "🚀 Instalando Tailscale via winget..."
          winget install --id=Tailscale.Tailscale --accept-package-agreements --accept-source-agreements -h | Out-Null

          $tsPath = "C:\Program Files\Tailscale\tailscale.exe"
          if (-not (Test-Path $tsPath)) {
            throw "❌ Falha ao instalar Tailscale"
          }

          $authkey = '${{ secrets.TAILSCALE_AUTH_KEY }}'
          if (-not $authkey) {
            throw "❌ ERRO: Nenhuma auth key definida em TAILSCALE_AUTH_KEY (secrets)"
          }

          Write-Host "🔗 Conectando ao Tailscale automaticamente..."
          & "$tsPath" up --authkey "$authkey" --hostname "gh-runner-${{ github.run_id }}" --accept-routes --ssh
          
          if ($LASTEXITCODE -ne 0) {
            throw "❌ Falha ao conectar ao Tailscale"
          }

          # Capturar IP
          $tsIP = (& "$tsPath" ip -4 | Select-Object -First 1).Trim()
          if (-not $tsIP) {
            throw "❌ Nenhum IP Tailscale encontrado"
          }

          Write-Host "✅ Conectado ao Tailscale com IP: $tsIP"
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      # ---------------------------
      # 4️⃣ Testar conexão RDP via Tailscale
      # ---------------------------
      - name: 🔍 Testar acesso RDP
        shell: pwsh
        run: |
          $tsIP = $env:TAILSCALE_IP
          Write-Host "🖧 Testando acesso a $tsIP:3389 ..."
          $test = Test-NetConnection -ComputerName $tsIP -Port 3389
          if (-not $test.TcpTestSucceeded) {
            throw "❌ Porta RDP 3389 inacessível via Tailscale"
          }
          Write-Host "✅ Porta RDP 3389 acessível via Tailscale"

      # ---------------------------
      # 5️⃣ Exibir informações e manter VPS ativa
      # ---------------------------
      - name: 🕓 VPS Ativa e Informações
        shell: pwsh
        run: |
          Write-Host ""
          Write-Host "================== RDP ACCESS =================="
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          Write-Host "Usuário: RDP"
          Write-Host "Senha: ${{ secrets.RDP_PASSWORD }}"
          Write-Host "================================================"
          Write-Host ""
          Write-Host "💡 Conecte-se via Tailscale + RDP (IP acima)."
          Write-Host "🌐 O Tailscale já está autenticado automaticamente."
          Write-Host "⚡ VPS ativa até cancelamento manual."
          Write-Host ""

          while ($true) {
            Write-Host "[$(Get-Date)] VPS ativa e aguardando conexão..."
            Start-Sleep -Seconds 300
          }
